{
  "name": "@satusd/types-ref",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript reference for SatUSD canonical encoding and hashes (PRD §6, §18).",
  "scripts": {
    "verify": "node src/verify.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
