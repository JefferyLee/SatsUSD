# Rust
/target
**/target
Cargo.lock.bak

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Editor / OS
.DS_Store
*.swp
.vscode/
.claude/

# Local env
.env
.env.local

# Devnet runtime (binaries, chain/wallet data, logs) — not committed
devnet/bin/
devnet/data/
devnet/logs/

# Pre-SatUSD 2023 legacy files, archived out of the way (kept on disk, not tracked)
legacy/

# Signet runtime (chain/wallet data, logs) — not committed
signet/data/
signet/logs/
